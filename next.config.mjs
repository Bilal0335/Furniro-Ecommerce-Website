/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["cdn.sanity.io"], // Ensure this matches the Sanity image domain
    },
  };
  
  export default nextConfig;
  